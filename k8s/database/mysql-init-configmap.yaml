apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init-scripts
  namespace: laravel-app
data:
  init.sql: |
    -- Create testing database for Laravel
    CREATE DATABASE IF NOT EXISTS `laravel_testing`;
    GRANT ALL PRIVILEGES ON `laravel_testing`.* TO '${MYSQL_USER}'@'%';
    FLUSH PRIVILEGES;
