apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: laravel-app

resources:
- ../../base
- ../../database

patchesStrategicMerge:
- deployment-patch.yaml
- configmap-patch.yaml

images:
- name: your-registry/laravel-app
  newTag: dev-latest
